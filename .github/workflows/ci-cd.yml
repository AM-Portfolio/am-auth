# DEPRECATED — Replaced by am-auth-publish.yml
# This file is intentionally disabled. Do not add jobs here.
name: DEPRECATED CI-CD
on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'This workflow is deprecated. Use am-auth-publish.yml instead.'
        required: false
jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. Please use am-auth-publish.yml"
