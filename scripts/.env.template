# Vault orchestrator — copy to scripts/.env (never commit .env)
# Used by: npm run vault:backup | vault:sync | vault:provision

VAULT_TUNNEL_PORT=8201
VPS_KUBECONFIG=../VPS/kubeconfig.vps

VAULT_ADDR=http://localhost:8201
VAULT_TOKEN=hvs.YOUR_ROOT_TOKEN
VAULT_UNSEAL_KEY_B64=YOUR_UNSEAL_KEY_B64

# VAULT_BACKUP_DIR=vault/backups
# VAULT_BLUEPRINT_PATH=vault/blueprints/v3_master.json
# VAULT_POLICY_DIR=vault/policies
