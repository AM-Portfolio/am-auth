{{- if and .Values.authTokens.enabled .Values.global.isLocal }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: am-auth-tokens
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "am-auth.labels" . | nindent 4 }}
    app.kubernetes.io/name: am-auth-tokens
spec:
  replicas: {{ .Values.authTokens.replicaCount }}
  selector:
    matchLabels:
      app: am-auth-tokens
  template:
    metadata:
      labels:
        app: am-auth-tokens
        {{- include "am-auth.labels" . | nindent 8 }}
    spec:
      containers:
        - name: am-auth-tokens
          image: "{{ .Values.global.image.registry }}/{{ .Values.authTokens.image.repository }}:{{ .Values.global.image.tag }}"
          imagePullPolicy: {{ .Values.global.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.authTokens.port }}
          command: ["/bin/sh", "-c"]
          args:
            - |
              exec uvicorn main:app --host 0.0.0.0 --port {{ .Values.authTokens.port }}
          env:
            - name: ENVIRONMENT
              value: {{ .Values.environment | quote }}
            - name: LOG_FORMAT
              value: "json"
            - name: LOG_LEVEL
              value: "INFO"
            {{- if .Values.authTokens.env }}
            {{- range $k, $v := .Values.authTokens.env }}
            - name: {{ $k | quote }}
              value: {{ $v | quote }}
            {{- end }}
            {{- end }}
          resources:
            {{- toYaml .Values.authTokens.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.authTokens.port }}
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.authTokens.port }}
            initialDelaySeconds: 20
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
{{- end }}
