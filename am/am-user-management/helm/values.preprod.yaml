global:
  image:
    registry: ghcr.io/am-portfolio
    pullPolicy: Always
  vault:
    enabled: true
    address: "http://vault.vault.svc.cluster.local:8200"
    role: "am-backend-role"
    authPath: "auth/kubernetes"
    serviceAccountName: "am-backend-sa"
    secrets:
      jwt:      "secret/data/preprod/apps/auth/jwt"
      postgres: "secret/data/preprod/infra/postgres"
      redis:    "secret/data/preprod/infra/redis"

environment: preprod
replicaCount: 1
language: python
image:
  repository: am-user-management
port: 8000
entrypoint: "exec uvicorn main:app --host 0.0.0.0 --port 8000"

env:
  ENABLE_EMAIL_VERIFICATION: "false"
  ALLOW_REGISTRATION: "true"
  MAX_LOGIN_ATTEMPTS: "5"
  ACCOUNT_LOCKOUT_MINUTES: "15"
  LOG_FORMAT: "json"
  LOG_LEVEL: "INFO"
