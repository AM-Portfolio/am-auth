# Preprod Environment — am-api-gateway
# Vault path aligned with seeded secret at kv/preprod/apps/auth
environment: preprod
replicaCount: 1

global:
  vault:
    secrets:
      auth: "kv/data/preprod/apps/auth"
