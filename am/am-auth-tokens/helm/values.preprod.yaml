# Preprod Environment — am-auth-tokens
# Vault path aligned with seeded secret at kv/preprod/apps/auth
environment: preprod
replicaCount: 1

global:
  vault:
    secrets:
      auth: "apps/data/preprod/services/am-auth"
      infra: "apps/data/preprod/infra/postgres"
