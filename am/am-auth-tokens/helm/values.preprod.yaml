# Preprod Environment — am-auth-tokens
# Vault path aligned with seeded secret at kv/preprod/apps/auth
environment: preprod
replicaCount: 1

vault:
  enabled: true
  secretPaths:
    auth:
      path: "apps/data/preprod/services/am-auth"
    postgres:
      path: "apps/data/preprod/infra/postgres"
