global:
  image:
    registry: ghcr.io/am-portfolio
    pullPolicy: Always
  vault:
    enabled: true
    address: "http://vault.vault.svc.cluster.local:8200"
    role: "am-backend-role"
    authPath: "auth/kubernetes"
    serviceAccountName: "am-backend-sa"

image:
  repository: am-auth-tokens
port: 8001
language: python

env:
  LOG_FORMAT: "json"
  LOG_LEVEL: "INFO"
  TZ: "UTC"
